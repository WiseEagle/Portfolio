
let defaultState = [
    {id:1,blockName:"Контакти",content:"0679770411 (telegram), WiseEagleSC@gmail.com"}
];

let contactsPageReducer = (state = defaultState, action) => {
    return state;
}

export default contactsPageReducer;
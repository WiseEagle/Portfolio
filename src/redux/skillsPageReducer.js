
let defaultState = [
    {id:1,blockName:"Мої навички",content:"HTML, CSS, jQuery, wordpress, joomla, modx, cs-cart, react"}
];

let skillsPageReducer = (state = defaultState, action) => {
    return state;
}

export default skillsPageReducer;
